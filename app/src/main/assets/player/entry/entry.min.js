// Placeholder for EntryJS runtime.
//
// To run the offline Entry player, download the official EntryJS distribution
// and copy the `entry.min.js` along with any required dependencies into this
// directory. See https://github.com/entrylabs/entryjs for details.
//
// The player.js script expects EntryJS to expose a global `Entry` object
// with methods such as `loadProject`, `engine.start`, and `engine.toggleStop`.